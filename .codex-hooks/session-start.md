# Session Start Reminder

Primary task: build a presentation notebook analyzing DUUI-Py from author ergonomics through events/telemetry and descriptor-generated MsgPack/Lua async evaluation.

Required order:

1. Confirm code and data artifacts.
2. Build notebook sections from the protocol.
3. Plot latency, tail latency, throughput, CPU, memory, and network only from available data.
4. Keep terminology stable: `baseline` and `async variants`.
5. Avoid code changes unless explicitly requested.
