# Pre-Compression Reminder

Continue the DUUI-Py presentation work from `docs/duui-py-presentation-protocol.md`.

Do not make unsupported claims. Before writing a notebook section, verify the relevant code path or data artifact directly. The evaluation sections must use `baseline` versus `async variants`, and the report must be neutral research-paper English with no fourth-wall phrasing.

Next durable checkpoint belongs in `docs/duui-py-presentation-protocol.md` or `~/.codex/session-notes/projects/duui-alpha.md`.
