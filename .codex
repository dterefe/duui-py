# Codex Reminder

See `docs/duui-py-presentation-protocol.md` before continuing the presentation notebook work.

The report must remain evidence-based, neutral, and structured around `baseline` versus `async variants`.
