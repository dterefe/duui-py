# GNFinder Legacy Lua

DUUI-py migration of the old `TTLab-UIMA/GNFinder` XMI passthrough Lua contract. It reads legacy XMI requests, runs the GNFinder binary, adds legacy `org.texttechnologylab.annotation.type.Taxon` annotations, and returns XMI.
