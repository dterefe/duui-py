# GeoNames Legacy Lua

DUUI-py migration of the old `gazetteer-rs/variants/geonames` custom Lua contract. It preserves the legacy match-label parsing contract used by `org.texttechnologylab.annotation.GeoNamesEntity`.
