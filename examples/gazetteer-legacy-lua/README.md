# Gazetteer Legacy Lua

DUUI-py migration of the old `gazetteer-rs/variants/biofid` custom Lua contract. It proxies a gazetteer-rs compatible backend and preserves the legacy list response consumed by the Lua script.
